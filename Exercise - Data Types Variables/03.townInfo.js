function solve(town, population, area)
{
    let townName = town;
    let populationOfTown = population;
    let townArea = area;
    console.log(`Town ${townName} has population of ${populationOfTown} and area ${townArea} square km.`);
}
solve('Sofia', 1286383, 492)
