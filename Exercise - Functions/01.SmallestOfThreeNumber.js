function smallestNum(a, b, c){
    let firstNum = a;
    let secondNum = b;
    let thirdNum = c; 

    let minNum = Math.min(firstNum, secondNum, thirdNum);
    console.log(minNum)
}
smallestNum( 2, 5, 3 )